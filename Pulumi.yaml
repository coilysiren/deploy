name: deploy
description: deployment for python API + react frontend, via pulumi and typescript
backend:
  url: gs://coilysiren-delpoy-pulumi-state
runtime:
  name: nodejs
  options:
    packagemanager: npm
config:
  pulumi:tags:
    value:
      pulumi:template: typescript
